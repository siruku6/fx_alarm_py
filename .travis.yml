language: python
python:
  - 3.8
env:
  global:
    - CC_TEST_REPORTER_ID=df0bec54ec6b21ab786dc05443fe9f52faa30df4b21c8fa2a2574b6bb47a1fb7
install:
  - pip install -r requirements.txt
  - pip install coverage
before_script:
  - cd /home/travis/build/siruku6/fx_alarm_py
  - curl -L https://codeclimate.com/downloads/test-reporter/test-reporter-latest-linux-amd64 > ./cc-test-reporter
  - chmod +x ./cc-test-reporter
  - ./cc-test-reporter before-build
script:
  - coverage run -m unittest discover
  - coverage xml
after_script:
  - ./cc-test-reporter after-build --prefix /home/travis/build/siruku6/fx_alarm_py --coverage-input-type coverage.py --exit-code $TRAVIS_TEST_RESULT
